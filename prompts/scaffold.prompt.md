---
description: "Cria scaffold de projeto com estrutura, configuração e boilerplate para qualquer linguagem/framework."
---

# Scaffold Project

Create a complete project scaffold for: **${input:description}**

- **Language/Framework**: ${input:stack}
- **Type**: ${input:type}

## Requirements
1. Generate the full directory structure
2. Create all configuration files (package.json, tsconfig, Cargo.toml, go.mod, pom.xml, etc.)
3. Set up linting and formatting configs
4. Create a minimal working entry point
5. Include a basic test setup with one example test
6. Add a .gitignore appropriate for the stack
7. Add a README.md with setup instructions
8. Follow the stack's community conventions and best practices

Create all files now — not just a directory tree.
